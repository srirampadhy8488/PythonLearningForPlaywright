bug_severities = ["Critical", "Minor", "Major", "Trivial", "Critical", "Major"]
# Loop through the list and print each severity
for severity in bug_severities:
    if severity == "Critical":
        print(severity)